services:
  - type: web
    name: tancare
    plan: free
    region: ohio
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      - key: APP_KEY
        generateValue: true
    healthCheckPath: /